"use client";
import Link from "next/link"; import { useRouter } from "next/navigation"; import { useState, type ReactNode } from "react";
export function RegisterForm(){const r=useRouter();const [form,setForm]=useState({name:"",specialty:"",email:"",phone:"",password:""});const [error,setError]=useState("");function submit(e:React.FormEvent){e.preventDefault();if(Object.values(form).some(v=>!v.trim()))return setError("Please complete all required fields.");if(form.password.length<6)return setError("Password must be at least 6 characters.");localStorage.setItem("schedula-doctor-account",JSON.stringify(form));r.push("/doctor/login");}return <AuthCard title="Create your doctor account" subtitle="Personal, professional and account details in one secure registration."><form onSubmit={submit} className="auth-form"><label>Full name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Specialty<input value={form.specialty} onChange={e=>setForm({...form,specialty:e.target.value})}/></label><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Phone<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Password<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{error&&<div className="error">{error}</div>}<button className="btn primary full" type="submit">Register and continue</button></form><p>Already registered? <Link href="/doctor/login">Sign in</Link></p></AuthCard>}
export function LoginForm(){const r=useRouter();const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");function submit(e:React.FormEvent){e.preventDefault();if(!email||!password)return setError("Enter your email and password.");r.push("/doctor/dashboard");}return <AuthCard title="Welcome back, Doctor" subtitle="Sign in to manage appointments, availability and prescriptions."><form onSubmit={submit} className="auth-form"><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="error">{error}</div>}<button className="btn primary full">Sign in</button></form><p>New here? <Link href="/doctor/register">Create doctor account</Link></p></AuthCard>}
function AuthCard({title,subtitle,children}:{title:string;subtitle:string;children:ReactNode}){return <main className="auth-page"><div className="auth-card"><Link href="/" className="brand">Schedula<span>+</span></Link><div className="eyebrow">Doctor portal</div><h1>{title}</h1><p>{subtitle}</p>{children}</div></main>}
