"use client";
import Link from "next/link";
import { usePathname, useRouter } from "next/navigation";
import { clearDoctorSession,getDoctorSession } from "@/features/doctor-portal/storage";
import { NotificationBell } from "@/features/notifications/NotificationBell";
const links=[{href:"/doctor/dashboard",label:"Dashboard"},{href:"/doctor/calendar",label:"Calendar"},{href:"/doctor/profile",label:"Profile & Slots"},{href:"/doctor/appointments",label:"Appointments"}];
export function DoctorPortalHeader(){const pathname=usePathname();const router=useRouter();const doctorId=typeof window!=="undefined"?getDoctorSession()||undefined:undefined;return <header className="sticky top-0 z-30 border-b border-[var(--line)] bg-white/95 backdrop-blur"><div className="mx-auto flex min-h-16 max-w-7xl items-center justify-between gap-2 px-3 sm:px-6 lg:px-8"><Link href="/doctor/dashboard" className="flex shrink-0 items-center gap-2"><span className="grid size-9 place-items-center rounded-xl bg-[var(--brand)] font-bold text-white">S</span><span className="hidden sm:block"><span className="block text-[15px] font-bold">Schedula</span><span className="text-xs text-[var(--muted)]">Doctor portal</span></span></Link><nav className="flex items-center gap-1 overflow-x-auto" aria-label="Doctor portal navigation"><Link href="/doctors" className="hidden rounded-lg px-2 py-2 text-xs font-semibold text-[var(--muted)] hover:bg-stone-100 xl:block">Patient Portal</Link>{links.map(item=><Link key={item.href} href={item.href} className={`whitespace-nowrap rounded-lg px-2 py-2 text-[11px] font-semibold sm:px-3 sm:text-sm ${pathname===item.href?"bg-emerald-50 text-[var(--brand)]":"text-[var(--muted)] hover:bg-stone-100 hover:text-[var(--ink)]"}`}>{item.label}</Link>)}<NotificationBell audience="doctor" doctorId={doctorId}/><button type="button" onClick={()=>{clearDoctorSession();router.push("/doctor/login")}} className="rounded-lg border border-[var(--line)] px-2 py-2 text-[11px] font-semibold hover:border-[var(--brand)] hover:text-[var(--brand)] sm:px-3 sm:text-sm">Sign out</button></nav></div></header>}
